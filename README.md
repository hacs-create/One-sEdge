
  # One's edge

  This is a code bundle for One's edge. The original project is available at https://www.figma.com/design/m1JNhn2S5OI4UhC3ArIvfF/One-s-edge.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  